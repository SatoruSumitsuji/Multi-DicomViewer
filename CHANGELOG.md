# 変更履歴 (CHANGELOG)

Multi-DICOMviewer のバージョンごとの主な機能・修正の履歴です。
日付は各バージョンのタグ作成日（JST）。最新が上です。

各リリースのビルド済みアプリ（Windows / macOS）は
[GitHub Releases](https://github.com/SatoruSumitsuji/Multi-DicomViewer/releases)
からダウンロードできます。

---

## 1.11.0 — 2026-06-30

**新機能（UI）**
- **Studies に「Delete All」ボタン**（「DICOM Info」の右）：一覧の全 study/series を
  まとめてリストから削除します（右クリックの Delete と同じく**元のファイルは消えません**。
  フォルダを読み込み直せば復元できます）。確認ダイアログ付き。
- **上部に「Clear All」ボタン**（「Layout」の右）：全ペインの表示画像を一括クリア
  （各ペインの「✕」と同じ。レイアウトと一覧は保持）。
- **Tree の Study をペインへドラッグ＆ドロップ**して表示できるようにしました
  （ペインを選んでから Tree で選ぶ手順が不要に。シリーズの D&D も従来どおり）。
- **マルチペインの「1×1」ボタンを廃止**（未使用のため）。ペインのダブルクリックでの
  最大化と、上部「Layout 1×1」は従来どおり使えます。
- **シリーズ位置カウンタ「現在/総数」を追加**（Angio/IVUS のフレーム数の右、CT は
  2D スライススクラバー内）。**太字・1.5倍**で見やすく表示。
- **計測時のツールボタン表示**：計測の種類（Line/Polyline 等）を選ぶと左クリックは計測に
  使われるため、**Zoom/Move 等のツールボタンをグレイアウト**（選択中は赤を残した薄赤）。
  種類未選択のあいだは従来どおりツールを使えます。

**修正（macOS 3DCT）**
- **角度を手動設定した後、トラックパッドでツールバーのボタンが効かなくなる不具合を修正**
  （角度ダイアログのモーダル入れ子イベントループが macOS のトラックパッド入力経路を奪う
  のが原因。**非モーダル化**して解消）。あわせて角度ダイアログ再表示時のクラッシュも修正。
- 角度ダイアログの **CRA/CAU・LAO/RAO のラベル見切れ**を修正。

**修正（DICOM 読み込み／表示）**
- 同一 SeriesUID 内に **8bit と 16bit の単フレーム静止画が混在**するシリーズで
  アプリが落ちる不具合を修正（XA の W/L ルックアップを全プレーンの値域に対応）。
- パックド XA で **ツリーの SeriesNo が全て「#1」**になる不具合を修正
  （InstanceNumber が区別にならない場合は実 SeriesNumber を表示）。
- **フォルダドロップ時の自動オープン**が、レポート等の Secondary Capture 静止画ではなく
  **再生可能なシネ**を優先するようにしました。
- Tree のサムネイル表示中、**ターゲットペインの変更にサムネイルが追従**し、
  **空ペイン選択時は空白**になるよう修正。

---

## 1.10.0 — 2026-06-26

**新機能（画質トグル）**
- **Angio / IVUS に「S-Cine」「S-Zoom」**（Lanczos による平滑化）と **「Denoise」**
  （エッジ保持ノイズ低減）を追加しました。ユーザーが任意に ON/OFF できます。
- **macOS の 3DCT に「HQ-Img」トグル**：拡大・回転・移動などの操作中も MPR を
  **フル解像度で再構成**し続けます（操作中の粗いプレビューを無効化）。きれいになる
  一方で GPU・メモリ負荷が上がるため、**既定は OFF**、**Apple Silicon・16GB以上を推奨**。
  - **8GB の Mac（主に旧世代 M1/M2）** では、大きな CT データ（薄スライス・多枚数）で
    動作が重くなったり一時的に固まる場合があります。その際は HQ-Img を OFF にしてください。
    詳細は [`docs/HQ-IMG_notice.md`](docs/HQ-IMG_notice.md)。
  - なお **macOS ビルドは Apple Silicon 専用（arm64）**で、**Intel Mac は非対応**です。

**改善（UI）**
- **macOS の HQ-Img ボタンを Plane 段の最左**（「Plane:」ラベルより左）へ移動し、
  最も目立つ位置にしました。ON のときは青く表示されます。
- CT の Plane ボタンが読み込み時に既定で **「Bi」** になるよう修正（以前は誤って「Rt」）。

**メモ**
- macOS 配布ビルドは **Developer ID 署名＋Apple 公証＋ステープル済み**です。**初回起動は
  通常のダブルクリックでOK**（以前の `xattr` 回避策＝「呪文」は不要）。

---

## 1.9.0 — 2026-06-23

**修正（3DCT）**
- **3DCT 左ペインの緑▲（センターライン）の向きの不具合を修正**しました（Windows・macOS 両対応）。

---

## 1.8.5 — 2026-06-22

**改善（ペイン操作）**
- **マルチペインから 1×1 表示への切り替えを簡単に**しました。これまでは「Layout 1×1」を選ぶ
  必要がありましたが、次の2つの方法を追加しました。
  - 各ペインのタイトルバー右側（✕ の左）の **「1×1」ボタン**をクリック。
  - ペイン上の **任意の場所をダブルクリック**（上部の濃い灰色のタイトル帯・画像領域いずれも可。
    `1×1` / `✕` ボタンの上だけは対象外）。1×1 表示中は無効なので、計測確定などのダブルクリック
    操作と干渉しません。
- **Layout ピッカーのセル色をデータの有無で区別**するようにしました。現在の表示範囲は青、
  **データが入っているペインは明るい灰色**、**空のペインは暗い灰色**で表示します。どこまで開けば
  全データペインが表示できるかが一目で分かります。

---

## 1.8.4 — 2026-06-20

**新機能**
- **File ▸ 「Open DICOM file…」（Ctrl+Shift+O）を追加**しました。フォルダ全体ではなく、個別の
  DICOM ファイル（複数選択可）を開けます。
- **Tools ▸ 「Export binary DICOM tag…」を追加**しました。バイナリ VR タグ（OB/OW/UN — タグ
  ビューアでは `<binary>` としか表示されないもの）を3形式でテキスト書き出しできます。
  - 対象ファイルは **「Select」ボタン → 4×3 のペイングリッド**から、現在開いているペインを
    クリックして選びます。
  - タグを入力して **Read**（VR・バイト数・印字可能率を表示）→ 形式を選んで **Export**:
    - `<stem>.<GGGG_EEEE>.hex.txt`（16進ダンプ）
    - `<stem>.<GGGG_EEEE>.base64.txt`（Base64・可逆）
    - `<stem>.<GGGG_EEEE>.latin1.txt`（latin-1 1:1・埋め込みテキスト確認）

---

## 1.8.3 — 2026-06-19

**修正（カラー表示）**
- **カラーの "Processed Images"（GE Volume Rendering など）が緑被り／グレイ表示になる不具合を
  修正**しました。PhotometricInterpretation が RGB と誤記された JFIF-YCbCr の JPEG を、JFIF
  マーカーに従って正しくデコードするようにしました（例: CT Series #302/#303 が黒背景＋ピンクの
  心臓で正しく表示されます）。他のカラー／グレイ画像の表示には影響しません。

---

## 1.8.2 — 2026-06-19

**改善（起動）**
- **起動直後にスプラッシュ画面を表示**するようにしました。重い DICOM スタック（pydicom など）の
  読み込み中も「Multi-DICOMviewer / 起動中… / Loading…」が即表示されるため、「起動しない？」と
  待たされる不安を解消します。
  - **Windows**: PyInstaller のネイティブスプラッシュで、Python 読み込み前の最初の数秒から表示。
  - **macOS / 開発実行**: Qt スプラッシュで表示（元々瞬時起動のため一瞬で消えます）。

---

## 1.8.1 — 2026-06-19

**改善（起動）**
- **Windows の初回起動を高速化**しました。CT 描画バックエンド（Windows=VTK / Mac=pygfx）を
  起動時ではなく**最初に CT を開いたとき**に読み込む遅延ロードに変更。アプリ画面が即表示され、
  XA/IVUS のみ使う場合は CT の読み込みコストを払いません。

**改善（表示）**
- **DICOM タグの黒縁取りを全モダリティに適用**しました（XA / IVUS / US など）。明るい/白い
  画像の上でも白タグが読みやすくなります（CT は 1.8.0 で対応済み）。

**改善（Tools）**
- **DicomCheck**: 下部の操作ボタン（Select all / Deselect all / Delete selected files +
  empty folders）の文字を 1.5 倍に拡大。
- **DicomFolder**:
  - 出力フォルダ未選択の `(none)` を、**ソース選択後は太字の赤**で強調（ソース未選択時は灰色）。
  - 「Organize」を **「Sort Files」に改名・1.5 倍**に拡大。出力フォルダ未設定時はグレイアウトし、
    左に赤字「Output folder is not selected」を表示（ソース選択後のみ）。
  - **「Clear Selection」ボタンを追加**（全選択をクリアして初期状態に戻す。ソース未選択時は
    グレイアウト）。
  - **`@STILL` の単一フレーム分割を XA のみ**に変更。XA のスポット撮影だけを `XA@STILL;日付`
    に分離し、その他のモダリティ（US/CT/MR/NM 等）は 1 枚画像も `検査名;日付` に分類します。
    チェックボックスも「Separate XA single-frame (XA@STILL)」に改名。

---

## 1.8.0 — 2026-06-18

**新機能（心臓CT 3D）**
- **角度表示（RAO/CRA）を右クリック → 任意の C-arm 角度へ回転**できるようにしました。
  画面下中央の角度表示を右クリックすると **LAO/RAO（0〜180°）と CRA/CAU（0〜90°）**を
  入力するダイアログが開き、**センターライン交点を中心に**その角度の投影へ回転します。
  対応するアンギオ像に合わせた角度での評価に使えます。

**改善（表示）**
- **角度表示テキストを 1.2 倍に拡大**し、**黒の縁取り**を付けました。背景が白い断面でも
  黄色文字が読めるようになります。
- **DICOM タグ（白文字）に細い黒の縁取り**を付け、明るい/白い断面でも視認できるように
  しました（文字サイズを変えてもきれいに表示されます）。

**修正（心臓CT 3D）**
- **初期表示で片側のセンターラインを回すと反対ペインが上下反転する不具合を修正**しました
  （連動ペインの法線符号を前フレームと連続になるよう選択）。
- **片側のセンターラインを回すと、もう片側のセンターライン／画像が直立にリセットされる
  挙動を修正**しました。連動ペインの向きを連続に保ち、センターラインは 2 平面の交線を
  追従するようにしたので、勝手に戻らなくなります。

---

## 1.7.0 — 2026-06-18

**修正（心臓CT 3D）**
- **初期表示の向きを解剖学的に正しく**しました（patient_basis 基準）。最初の軸位像は
  「下から見上げる」向きで、**画面右＝患者の左・画面上＝胸骨（前面）・画面下＝脊椎（背面）**
  になり、画像右に左室・画像左に右室が見えます。
- **回転中に反対側ペインが突然 上下／左右反転する不具合を修正**しました
  （連動ペインの法線符号を前フレームと連続になるよう選択）。
- **角度表示が逆向きを正しく表すように修正**しました。センターラインで反対側の描画方向を
  完全に反転させた場合、従来 LAO30 のまま折り返していたのを **RAO150 CRA0** と表示します。

**修正（表示）**
- **アンギオ画像の DICOM タグ表示の灰色背景を削除**しました（視認性向上のため背景なしに）。
- **再生不可（1 フレーム）のときシークバーの中央ドットを青→灰**にしました。Play ボタンの
  グレイアウトと見た目を揃え、アクティブと誤認しないようにしました。

**修正（MR / NM）**
- **符号付き 16bit（MR PSIR など）の画像でアプリが落ちる不具合を修正**しました
  （ウィンドウ LUT の整数オーバーフロー。NumPy 2 / NEP50 環境）。
- **複数画像の MR / NM シリーズを 2D シネとして表示・再生**できるようにしました。
  1 ファイル＝1 フレームで保存されたスタックを 1 シリーズにまとめ（従来は 1 枚ずつ
  別シリーズに分割）、Play／シークで連続再生できます（SSMview と同様の 25〜35 枚シネ）。

**機能追加（Tools）**
- **DicomCheck** を追加。フォルダを走査して **非 DICOM ファイル・空フォルダを判定して削除**
  します（旧 HTML 版をネイティブ実装。フォルダのドラッグ＆ドロップ対応）。
- **DicomFolder** を追加。**DICOM タグで日付／モダリティ／検査別にファイルを整理**します
  （旧 Electron 版をネイティブ実装。フォルダのドラッグ＆ドロップ対応）。
  CT／MR／NM は本来単一フレームのため、`@STILL` 分離の対象外にしています。

---

## 1.6.9 — 2026-06-17

**修正（表記の統一）**
- DICOM情報ボタンの右クリック・ツールチップの文言を **「tag data」に統一**しました
  （メイン画面側が「tag items」、Tree側が「tag data」と分かれていたのを揃えました）。

---

## 1.6.8 — 2026-06-17

**変更（表記を英語に統一）**
- **アプリ内のユーザー向けメッセージ・ツールチップ・ダイアログをすべて英語化**しました。
  再生上限アラート、空フォルダのドロップ通知、各種ツールチップ（枠を閉じる／レイアウト選択／
  DICOM情報／Tree幅）、Anonymize 設定、MultiSync、Coaxial Eval、Rupture Predictor の
  文言を英語に統一（臨床表現は医師レビューの文言を反映）。ソースコメントは対象外です。

**機能追加（レイアウト）**
- **グリッド式レイアウトピッカーを追加**。最大 4×3（12ペイン）まで、タイルをなぞって
  分割数を直感的に選べます（従来の 1×2／2×1 などの個別ボタンを置き換え）。

**機能追加（同時再生ガード）**
- **シネ動画の同時再生を最大 4 画面に制限**。5 つ目を再生しようとするとアラートを表示します
  （多画面同時再生によるフリーズ・高負荷を予防）。

**修正（ナビゲーション）**
- **マルチ画面で First/Prev/Next/Last がアクティブなペイン自身のシリーズから移動**するように
  修正（意図しない画像へジャンプする問題を解消）。

**修正（ドラッグ＆ドロップ）**
- **空フォルダ／対象外フォルダをドロップした際に「Scanning」のまま固まる問題を修正**。
  表示できる DICOM が無い場合はメッセージを出してスキャンを終了します。

---

## 1.6.7 — 2026-06-17

**修正（高DPI / Tree 枠のサイズ変更）**
- **高DPI（例：1920×1080＋150% スケーリング）で Studies(Tree) ドックが広げられない問題を修正**。
  上部バーが幅を占有して中央領域の最小幅を押し上げていたのを切り離し、Tree を
  大幅に大きくできるようにしました（論理1280pxで最大約530→約1200px）。上部バーは
  幅が狭いときボタンが省略表示になります（左端＋ツールチップは残る）。
- **Tree ツールバーに ◀ / ▶ ボタンを追加**。クリックで Tree 枠を狭く／広く
  できます（細い仕切りをドラッグしにくい時の代替手段。仕切り自体の見た目・挙動は
  従来どおり＝細い線・あたった時だけ青）。

---

## 1.6.6 — 2026-06-16

**修正（プライバシー）**
- **Anonymous 選択時、ペイン上帯の患者名・日付をマスク**するようにしました。
  実際の患者名・検査日を隠し、固定文字列 **「Name」「Date」** を表示します
  （例：`● Pane 1 - Name - Date - 3/12`）。シリーズ/インスタンス番号は PHI では
  ないため残します。Anonymous の ON/OFF は即座に全ペインへ反映されます。

**修正（macOS の表示）**
- **Tree（ブラウザ）ツールバーのボタン表示を修正**しました。Info ドックを
  狭くした際にボタン左端（アイコン）が切れる／逆に幅が十分でもアイコンだけに
  潰れる問題を解消。幅があれば「アイコン＋文字」を全文表示し、狭いときは左から
  最大限（左端は常に表示）。

---

## 1.6.5 — 2026-06-16

**改善（ペインのタイトル表示）**
- ペイン上帯の**患者名をアルファベット表記のみ**に短縮しました。DICOM 患者名が
  「アルファベット=漢字=カタカナ」（`=` 連結）の場合に全表記をつないで長く
  なっていたのを、最初のアルファベット部分だけ表示します
  （例：`YAMADA^TARO=山田^太郎=ヤマダ^タロウ` → `YAMADA TARO`）。

---

## 1.6.4 — 2026-06-16

**変更（ペインのタイトル表示）**
- ペイン上帯の表示を
  「`● Pane N - 患者名 - YYYYMMDD - シリーズ番号/インスタンス番号`」に変更しました
  （例：`● Pane 1 - YAMADA TARO - 20260615 - 3/12`）。
  - **患者名** と **InstanceNo** を追加。種別・説明・画像枚数の表示は省略しました。
  - 区切りを長いダッシュ（—）から短い **「 - 」** に変更。
  - ビューア内部のタイトル（説明・フレーム数・種別）は従来どおりです。

---

## 1.6.3 — 2026-06-16

**改善（ペインのタイトル表示）**
- 各ペインのタイトルに**検査日（YYYYMMDD）を追加**しました。表示は
  「`● Pane N — YYYYMMDD — #シリーズ番号 種別 — 説明  [N img]`」となり、
  **1x1〜2x3 のすべてのペイン**に反映されます。日付は DICOM の StudyDate を
  そのまま（YYYYMMDD）使用し、無い場合はシリーズの取得日にフォールバック、
  いずれも無ければ日付を省略します。

---

## 1.6.2 — 2026-06-16

**修正（再起動ランチャーを配布版に同梱）**
- 1.6.1 で追加した「ワンクリック再起動ランチャー」は**ダウンロード版(zip)に含まれていません**でした（ソース版にのみ存在）。1.6.2 で**配布版に同梱**します:
  - Windows: 展開フォルダ内の `Multi-DicomViewer.exe` の隣に `restart_viewer.cmd`
  - macOS: `Multi-DicomViewer.app` と同じフォルダに `restart_viewer.command`
- ランチャーを**配布版(frozen)の exe/.app を直接起動する形**に対応（macOS は `open -n` 経由で確実に再起動）。ソース版でも従来どおり動作します。
- 使い方: 展開フォルダ内のランチャーを**デスクトップにショートカット/エイリアスとして一度置く**だけ。フリーズ時にダブルクリックで**強制終了＋再起動**（タスクマネージャ不要）。

---

## 1.6.1 — 2026-06-16

**修正（2x3など多ペインでのフリーズ／クラッシュ）**
- **MultiSync-Viewer 起動でアプリが落ちる不具合を修正**: 起動時に内部エラー
  （未定義参照）でプロセスが終了していたのを修正。1×2 等で IVUS を開いて
  Tools → MultiSync-Viewer が正常に開くようになりました。
- **多ペインでのスライダー操作フリーズを修正**: 上部「DICOM タグ文字サイズ」、
  小画面のシークバー、IVUS 長軸の幅ズーム、W/L のドラッグで固まる／描画が
  非常に重い問題を改善（描画の集約・可視範囲限定・コールドフレームの同期
  デコード回避）。
- **IVUS 長軸を高速化＆非ブロック化**: 長軸の合成を高速化し、暖め中でも操作可能に
  （未デコード部は黒列で表示され、順次埋まる）。

**変更（IVUS 長軸の表示条件）**
- **IVUS 長軸は単一画面（1x1）レイアウトでのみ表示可能に**: 小さな多ペイン枠では
  情報量が乏しく、かつフリーズの主因だったため、多ペイン（2x3 等）では Long View を
  無効化しました（1x1 では従来どおり）。

**追加（フリーズ時のワンクリック復帰）**
- **再起動ランチャーを同梱**: 万一フリーズした際に、タスクマネージャを使わず
  ワンクリックで強制終了＋再起動できるランチャーを追加
  （Windows: `restart_viewer.cmd` / macOS: `restart_viewer.command`）。
  デスクトップにショートカット／エイリアスを一度作成してご利用ください。

---

## 1.4.1 — 2026-06-12

**改善（シリーズの表示／非表示まわり）**
- **シリーズ右クリックの UnHide を「UnHide (show this series)」に変更**:
  これまで Study 全体の非表示を解除していましたが、**そのシリーズだけ**を解除する
  ように修正（複数選択時は選択分のみ）。
- **非表示シリーズもマウスクリック／ドラッグで表示可能に**: グレー表示の非表示
  シリーズでも、ツリー／サムネイルを直接クリック（またはペインへドラッグ）すれば
  その場で開けるようになりました。開いた後の Play・シークバーも通常どおり動作。
  **画像上の First/Prev/Next/Last ボタンとキーボードショートカット**（および
  キーボード Enter）では従来どおりスキップします。
- **Study 右クリックを再構成**:
  Select (show unhide only) / Show (show all with hide/unhide) /
  UnHide (unhide and show all) / Close (close list) / Delete (remove)。
  - Select=非表示シリーズの行を一覧から畳んで表示シリーズのみ表示（削除はしない）、
    Show=各シリーズの表示／非表示状態を保ったまま全シリーズの行を表示、
    UnHide=非表示を全解除して全シリーズ表示、Close=状態を保ったまま一覧を閉じる、
    Delete=Study ごと一覧から削除。
- **非表示シリーズ選択時のハイライトをグレー背景に**: 青の選択背景が強く、選択中だと
  グレー文字（非表示）かどうか判別しづらかったため、**非表示シリーズを選ぶと薄い
  グレー背景**、表示シリーズを選ぶと従来どおり青背景にしました。

---

## 1.4.0 — 2026-06-12

**新機能**
- **匿名化 DICOM 書き出し（Export (Anon DICOM)）**: 元ファイルを de-identify
  したコピーを書き出し。指定タグの値とすべての Private タグの値を空にします
  （タグ自体・ピクセル・UID・転送構文は保持するので、書き出したファイルもそのまま
  表示・再取り込みできます）。Export (DICOM) が出る全箇所（Studies一覧の右クリック、
  画像の右クリック）に追加。
  - **匿名化する項目は Anonymous ボタンの右クリック**で設定（DICOMタグのチェック
    リスト＋「Private タグの値も空にする」）。既定は患者・施設・日付など24項目＋
    Private（Modality と Specific Character Set は既定オフ）。設定は保存されます。
  - **画面表示の匿名化（Anonymous トグル）も同じ設定**でマスクするよう統一。
- **シリーズの表示／非表示**（PowerPoint「スライド非表示」風）: シリーズ右クリックの
  **Hide / UnHide**。非表示シリーズはツリー／サムネイルでグレー表示になり、左クリック
  やドラッグでは開かず、First/Prev/Next/Last のナビでもスキップされます（右クリック
  メニューは反応）。First/Last は最寄りの表示シリーズへ。
  - **Study 右クリック**を Select (UnHidden Only) / Close (close list) /
    Show (show all) / Delete (remove) に再構成。Select=そのStudyの非表示シリーズを
    一覧から除外、Show=非表示を全解除。
- **サムネイル「最小サイズ×10列」**: Thumbnail ボタンの右クリック（および初回の
  サムネイル表示）で、サムネを最小サイズにして約10個横並びになるようパネル幅を調整
  ── シリーズの抜けチェック用。サムネイル最小サイズも縮小（80→60px）。
- **Windows: ウイルス対策ソフト対処ガイド**（`docs/WINDOWS_ANTIVIRUS_ja` の
  md/HTML/PDF）を同梱。未署名アプリの誤検知時の対処を平易に説明。

---

## 1.3.0 — 2026-06-11

**新機能**
- **DICOMタグの CSV 書き出し**: 画面に表示中の DICOM-Tag 項目を CSV 出力
  （タグ名・タグ番号・内容、内容は省略なしの全文）。Studies一覧の右クリック
  （複数選択で一括）と画像上の右クリックの両方から実行可能。バイナリ系（OB等）も
  テキストなら全文デコード、本物のバイナリは要約表示。UTF-8(BOM付き)で Excel でも
  日本語が文字化けしません。
  - **バイプレーンは Lt/Rt の2列**で書き出し（タグ名・番号の右に左右別の値）。
- **画像右クリックのエクスポート拡張**: 静止画(PNG/JPEG/TIFF)に加え
  **DICOM / MP4 / CSV** を追加。対象は**右クリックした画像のみ**（バイプレーンは
  クリックした側のプレーンだけをロスレス DICOM / 単独 MP4 / そのプレーンのタグ CSV）。
  CT は MP4 なし、IVUS 長軸は静止画＋CSV のみ。
- **macOS 版がコード署名＋公証(notarization)済みに**: 本リリースより Mac の
  配布物は Apple の署名・公証付き。**xattr での quarantine 解除作業が不要**になり、
  ダブルクリックでそのまま起動できます。

**変更**
- **Bi/Lt/Rt をペインごとに独立化**: 上段の Bi/Lt/Rt バーを廃止し、各ビューア内に
  「Plane: Bi/Lt/Rt」を配置（XAバイプレーン／CTデュアルMPR）。1×2・2×2 でも
  各ペインで別々に選べ、Bi はどのレイアウトでも両表示。
- **CenterAngle のクリック順**を「端点 → もう一方の端点 → 弧の指定点」に統一
  （XA / IVUS / CT すべて。Rupture-Predictor と整合）。
- **IVUS 長軸**: 回転を Shift不要の縦ドラッグに変更。回転中は低解像度プレビューで
  滑らかに、離すと高精細で再構築。
- **CT 初期表示シリーズ**: フォルダ読み込み時、イメージ数 200枚以上の中で
  シリーズ番号が最大のものを表示（200枚以上が無ければ最小番号）。
- バイプレーンで Rt 選択時に Lt のタグ情報が出ていた不具合を修正。
- 上部バーのボタンが狭いと文字が見切れる問題を改善（先頭が読めるよう省略＋
  ツールチップで全文表示）。

---

## 1.2.0 — 2026-06-10

**新機能**
- **再生範囲の書き出し（XA/IVUS）**: Playシークバー上に開始/終了マーカー
  （青い下向き三角）を追加。ドラッグで範囲を指定でき、再生・シーク・MP4書き出しが
  その範囲内に限定される（DICOM書き出しは仕様上つねに全フレーム）。フレーム数表示
  「XXX/XXX」をクリックすると範囲を全域にリセット。
- **心電図(ECG)表示（XA、Wキー）**: DICOMの Waveform Sequence または
  レガシー Curve Data から波形を読み取り、アンギオ画像の下に表示。再生フレームに
  同期したカーソルが移動。波形が無いシリーズでは非表示。
  （診断ツール `tools/probe_ecg.py` で格納形式を確認可能）

**変更**
- アンギオツリーで XA系 Study をクリックすると近傍の OT系 Study に飛んでしまう
  不具合を修正（study_uid と modality 種別の組で解決するよう変更）。
- Play / W-L スライダーつまみの内側の青丸マークを入れ替え（大きい丸→Play、
  小さい丸→W/L。つまみ自体のサイズは不変）。
- **IVUS 長軸ズーム**: Shift不要の通常ドラッグで「幅のみ」拡大縮小に変更。
  高さは常に表示枠にフィット（縦スクロールバーなし）。回転は Shift+縦ドラッグ。
- **Mac版 CT**: 画像上の右クリックの挙動を分離。右シングルクリック＝静止画書き出し、
  右ダブルクリック＝高解像度化（full-quality 再構築）。
- CI(macOS): 任意の Developer ID 署名・notarization に対応（条件付きで自動実行）。

---

## 1.1.0 — 2026-06-09

- 全ビューワで画像上の右クリックから静止画書き出し（PNG / JPEG / TIFF）。

---

## 1.0.0 — 2026-06-08

**計測機能の刷新**
- 結果テキストの色を黄色に統一。
- Center-Angle（中心角）をアウトライン上に拘束し、マーカーをドラッグで移動、
  右クリックで削除、色分け表示。頂点は面積重心ベースに。
- 計測スタイルを CT から XA/IVUS にも展開。短軸ドリフト修正、Tags/History の並び調整。

**表示・オーバーレイ**
- CT のタグ/結果テキストをスライダー指定サイズ厳守の固定サイズで描画。
  タグは左40%・結果は右40%に word-wrap で配置。
- VTK 計測線の太さを devicePixelRatio でスケールし、視認性向上（さらに×1.2）。

**Mac版 CT**
- スラブMPRはドラッグ・ページング中は低品質LODで滑らかに、操作終了で自動的に
  高品質化（各種クリスプアップの信頼性修正）。右クリックで手動高品質化。

- バージョン番号をアプリ／パッケージ／バンドルで 1.0.0 に統一。

---

## 0.1.6 — 2026-06-07

- **MultiSync**: 全体同期シークバー、同期ポイントのUX、本格的な高品質MP4書き出し。
- ロード進捗テキストの "cine" 表記を修正（IVUS はシネファイルではないため）。

---

## 0.1.5 — 2026-06-06

- Mac: 非CTの DICOMタグオーバーレイの行高を拡大（累積で約 1.392倍）。

---

## 0.1.4 — 2026-06-06

- DICOMタグ: 白文字・Meiryo フォント統一・行間調整、サイズスライダー上限を 16 に。
- ツリー: 画像総数をファイル数ではなくフレーム数で集計。単一画像シリーズは Play を無効化。

---

## 0.1.3 — 2026-06-06

- 超音波のカラーJPEGシネにも高速 imagecodecs デコーダを適用（約 13〜23倍高速化）。
- DICOMタグ文字サイズをモダリティ間で統一し、ビューワごとのサイズスライダーを追加。
- 単一 SeriesUID に複数インスタンスを持つ US/OTHER シリーズをシネ単位の行に分割。

---

## 0.1.2 — 2026-06-06

- CT/Mac: ドラッグ・ページング中はスラブMIPの再構築をスキップする対話的
  LOD（Level of Detail）を導入し、低メモリ Mac でも滑らかに操作（パフォーマンス改善）。

---

## 0.1.1 — 2026-06-05

- カラーの二重変換（YBR→RGB）で JPEG 超音波／カラーシネが緑がかる不具合を修正。

---

## 0.1.0 — 2026-06-05 — 初回公開

**Mac版 CT のネイティブ対応（大型機能）**
- VTK の OpenGL→Metal ハングを回避するため、CT を pygfx（wgpu→Metal）で
  ネイティブ描画。Phase 0〜8 を実装（dual oblique MPR、QPainterオーバーレイ＋
  クロスヘア連動、SPIN、HUカラーマップ、計測、HU統計、スラブMIP、macOSでCT有効化）。

**機能**
- ネイティブ Rupture-Predictor（ブラウザ版の伸展比ツールを PyQt6 に移植）。
- Coaxial Eval（同軸性評価）機能、2x1 レイアウト、Ctrl-ドラッグでパン。
- MultiSync 書き出し改善、モダリティごとのエクスポート設定の記憶、プライベートタグ対応、
  UID をまたぐ biplane マージ、サムネイルの右クリックメニュー。
- 計測ツール: 斜め（回転可能）楕円、角度のクリック順、CT の下書きプレビュー＋履歴。
- DICOMタグオーバーレイのトグル（ボタン＋Qキー）と既定タグの表示・保存。

**UI / その他**
- シークバーつまみ（白丸＋青ドット）、Play ボタン拡大、タグフォント調整、
  Study ブラウザの右クリック「Close」など各種 UI 改善。
- GPLv3 LICENSE とサードパーティ表記を追加。
- IVUS の YBR_FULL_422 を Y チャンネルでグレースケール表示する修正。
